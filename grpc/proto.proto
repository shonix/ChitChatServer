syntax = "proto3";
option go_package = "ITUServer/grpc/proto";
import "google/protobuf/timestamp.proto";

message ClientMessage {
  google.protobuf.Timestamp timestamp = 1;
  string from = 2;
  string message = 3;
}

service ChitChat {
  rpc Chat (stream ClientMessage) returns (stream ClientMessage) {};
}